import '@testing-library/jest-dom'
import * as React from 'react'
import {render, fireEvent, screen} from '@testing-library/react'

describe('<Pokemon/>', () => {
  test('Does it show the images of Pokemon', () => {
    expect(1).toBe(1)
  })
})
